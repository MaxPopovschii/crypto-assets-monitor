{
  "name": "@crypto-monitor/notification-service",
  "version": "1.0.0",
  "description": "Service for sending notifications (email, push, etc.)",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "start": "node dist/index.js",
    "clean": "rm -rf dist",
    "lint": "eslint src --ext .ts --max-warnings 10"
  },
  "dependencies": {
    "@crypto-monitor/types": "*",
    "dotenv": "^16.3.1",
    "ioredis": "^5.3.2",
    "nodemailer": "^6.9.7",
    "pino": "^8.17.1",
    "pino-pretty": "^10.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.10.5",
    "@types/nodemailer": "^6.4.14",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.3.3"
  }
}
